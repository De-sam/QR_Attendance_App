</tbody>
    </table>
</div>

<!-- <table class="table table-bordered">
    <thead class="thead-dark">
        <tr>
            <th>Organization Name</th>
            <th>Company ID</th>
            <th>Created By</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        {% for organization in organizations %}
        <tr>
            <td>{{ organization.name }}</td>
            <td>{{ organization.code }}</td>
            <td>{{ organization.creator.username }}</td>
            <td>
                <a href="{{ url_for('add_location', org_id=organization.id) }}" class="btn btn-sm btn-primary">Add Location</a>
                <button onclick="confirmDelete('{{ organization.id }}')" class="btn btn-sm btn-danger">Delete</button>
            </td>
        </tr>
        {% endfor %}
    </tbody>
</table>

<script>
function confirmDelete(orgId) {
    if (confirm("Are you sure you want to delete this organization?")) {
        window.location.href = `/delete_organization/${orgId}`;
    }
}
</script> -->